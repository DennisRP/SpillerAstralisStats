# RAG examples

Keep only synthetic or otherwise redistributable Markdown examples in this directory.

Real copied article text belongs in the ignored `content/rag/articles/` directory and
must never be published under `dist`.
